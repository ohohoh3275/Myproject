<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="galleryMapper">
	<insert id="insert">
		<![CDATA[
			insert into myfile(gno, title, content, writer, filename, uploadpath)
			values (myfile_seq.nextVal, #{title}, #{content}, #{writer}, #{fileName}, #{uploadPath})
		]]>
	</insert>

	<select id="select" resultType="kr.co.vo.ImageVO">
		<![CDATA[	
			select gno, title, content, writer, bdate, filename, uploadpath from myfile 
			order by gno desc
		]]>

	</select>
</mapper>